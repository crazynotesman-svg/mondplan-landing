{
  "name": "mondplan-landing",
  "version": "1.0.0",
  "private": true,
  "description": "MondPlan - Biodynamic Moon Calendar. Static landing page (HTML + Tailwind CSS), built for Cloudflare Pages.",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./assets/tailwind.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.11",
    "tailwindcss": "^4.1.11"
  }
}
